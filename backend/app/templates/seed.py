"""Template seed data placeholder.

NOTE: This file previously contained INITIAL_TEMPLATES constant and seed_templates() function.
These were never integrated into the application startup or migrations.

TODO: If template seeding is needed, either:
1. Create an alembic data migration to seed templates
2. Create a management CLI command (e.g., `python -m app.cli seed-templates`)
3. Re-implement seed_templates() and call from lifespan startup

The backup with original template data is available at:
.tmp/current/backups/app-templates-seed.py.backup
"""
