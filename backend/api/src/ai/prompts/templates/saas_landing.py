"""Template-specific prompt for SaaS landing page webapp generation."""

SAAS_LANDING_PROMPT = """\
## SaaS Landing Page (React) — Template-Specific Instructions

### Тип проекта
React webapp — маркетинговый лендинг для SaaS-продукта. Одностраничное приложение.

### Уникальность (КРИТИЧНО)
- Каждый SaaS-лендинг должен выглядеть УНИКАЛЬНО — другие цвета, компоновка, стиль
- **Переопредели CSS custom properties** в index.css для создания палитры, соответствующей нише
- НЕ используй всегда одну и ту же сине-фиолетовую палитру
- Вдохновляйся РАЗНЫМИ стилями: Linear, Vercel, Stripe, Notion, Figma, Raycast — но НЕ копируй один и тот же

### Рекомендуемые секции (адаптируй под запрос)
1. **Header** — sticky, навигация, CTA-кнопка
2. **Hero** — заголовок (max 8 слов), подзаголовок, кнопки. Компоновку ВАРЬИРУЙ: центр, split, с визуальным элементом
3. **Клиенты/Логотипы** — опционально
4. **Возможности** — 3-6 feature-карточек. Стиль карточек ВАРЬИРУЙ каждый раз
5. **Как это работает** — шаги с номерами (опционально)
6. **Тарифы** — 2-4 тарифа, цены ВАРЬИРУЙ под нишу (НЕ всегда 990/2990/7990)
7. **Отзывы** — опционально
8. **CTA** — финальный призыв
9. **Footer**

Не все секции обязательны. Добавляй/убирай под конкретный запрос.

### Данные
- Уникальное название SaaS-продукта (НЕ *Flow, *Tech, *Hub)
- Тарифы с РАЗНЫМИ ценами каждый раз — подбирай под нишу
- Фичи с конкретными описаниями под нишу

### Структура файлов
- `src/pages/Home.tsx` — основная страница
- `src/components/landing/` — компоненты секций
- Каждый компонент < 100 строк

### Запрещённые CSS-приёмы
- НЕ используй clip-path на секциях, skewY()/skewX() на блоках
- Бейдж "Популярный": если absolute — родитель ОБЯЗАН иметь overflow-visible

### Кнопки
- CTA-кнопки: `<Button className="btn-gradient">Текст <ArrowRight /></Button>` — gradient от --primary
- Вторичные: `<Button variant="outline">Текст</Button>`
- ⚠️ ЗАПРЕЩЕНО передавать bg-cyan-*, bg-green-* и другие цветовые классы в className Button

### Навигация
- Anchor-ссылки на секции, smooth scroll
- Mobile: hamburger-меню
"""
